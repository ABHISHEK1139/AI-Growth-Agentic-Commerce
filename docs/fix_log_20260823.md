# Fix Log — Cross-Sell & Recommendation System

Fixed: 2026-08-23

## Backend (`services/recommendations/service.py`)
- Removed synthetic `rec_prd_*` / `rec_off_*` IDs (no DB fallback)
- Removed `COMPATIBILITY_RULES` emission (DB-only via `CrossSellEngine`)
- Replaced `contextlib.suppress(Exception)` with visible `raise`
- AOV from real `Offer.unit_price_minor`, not hardcoded `6499900`
- Metrics (`attach_rate`, `projected_aov`) computed from `Order`/`CheckoutItem`, not `42.5%` fiction
- `get_merchant_metrics`: no hardcoded `base_aov = 6499900`

## Frontend (`apps/web/src/app/cart/page.tsx`)
- `available_quantity`: uses real DB value (`recAvailQty`), not `> 0 ? recAvailQty : 1`

## Pipeline (`pipeline/build_catalog.py`)
- Confirmed 6-stage resumable pipeline; no synthetic IDs; `seeded_price_paise()` deterministic

## Architecture docs
- Updated to reflect DB-only recommendations, no synthetic fallback, real inventory verification
